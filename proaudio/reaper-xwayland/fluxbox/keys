# Minimal Fluxbox keys for REAPER environment
# No Mod4 (Super) — Umbriel owns that.
# No Ctrl bindings — REAPER uses Ctrl heavily.
# No F-keys except F2/F4 — REAPER uses F-keys for actions.

# --- Window movement & resizing ---
# NOTE: no Alt+click ops on windows — Alt+mouse is REAPER's own modifier for
# plugin/mouse actions. Move/resize via the titlebar (drag titlebar, or
# right-click it for the window menu).

# --- Titlebar ops ---
OnTitlebar Mouse1 :MacroCmd {Raise} {Focus} {ActivateTab}
OnTitlebar Move1  :StartMoving
OnTitlebar Mouse3 :WindowMenu

# --- Window cycling ---
Mod1 Tab :NextWindow {groups} (workspace=[current])
Mod1 Shift Tab :PrevWindow {groups} (workspace=[current])

# --- Close window ---
Mod1 F4 :Close

# --- Run dialog ---
Mod1 F2 :Exec fbrun

# --- Desktop clicks ---
OnDesktop Mouse1 :HideMenus
OnDesktop Mouse3 :RootMenu
